lints.workspace = true

[package]
name = "gix-worktree"
version = "0.56.0"
repository = "https://github.com/GitoxideLabs/gitoxide"
license = "MIT OR Apache-2.0"
description = "A crate of the gitoxide project for shared worktree related types and utilities."
authors = ["Sebastian Thiel <sebastian.thiel@icloud.com>"]
edition = "2024"
include = ["/src/**/*", "/LICENSE-*"]
rust-version = "1.88"

[lib]
doctest = false

[features]
default = ["attributes"]
## Enable support for the SHA-1 hash by enabling the respective feature in the `gix-hash` crate.
sha1 = ["gix-hash/sha1"]
## Enable support for the SHA-256 hash by enabling the respective feature in the `gix-hash` crate.
sha256 = ["gix-hash/sha256"]
## Instantiate stacks that can access `.gitattributes` information.
attributes = ["dep:gix-attributes", "dep:gix-validate"]
## Create the administrative files for linked worktrees.
create = ["dep:gix-validate"]
## Remove linked worktrees recursively, with parallel traversal and deletion.
remove = ["dep:dua-core", "dep:thiserror", "gix-features/progress"]
## Data structures implement `serde::Serialize` and `serde::Deserialize`.
serde = ["dep:serde", "bstr/serde", "gix-index/serde", "gix-hash/serde", "gix-object/serde", "gix-attributes?/serde", "gix-ignore/serde"]
## Enable thread-safety.
parallel = ["gix-features/parallel", "gix-attributes?/parallel"]

[dependencies]
gix-index = { version = "^0.55.0", path = "../gix-index" }
gix-fs = { version = "^0.22.1", path = "../gix-fs" }
gix-hash = { version = "^0.26.1", path = "../gix-hash" }
gix-object = { version = "^0.64.0", path = "../gix-object" }
gix-glob = { version = "^0.27.1", path = "../gix-glob" }
gix-path = { version = "^0.12.6", path = "../gix-path" }
gix-attributes = { version = "^0.35.0", path = "../gix-attributes", optional = true }
gix-validate = { version = "^0.11.4", path = "../gix-validate", optional = true }
gix-ignore = { version = "^0.22.1", path = "../gix-ignore" }
gix-features = { version = "^0.49.1", path = "../gix-features", optional = true }
dua-core = { version = "3.3.0", optional = true }
thiserror = { version = "2.0.18", optional = true }

serde = { version = "1.0.114", optional = true, default-features = false, features = ["derive"] }
bstr = { version = "1.12.0", default-features = false }

document-features = { version = "0.2.0", optional = true }

[dev-dependencies]
gix-hash = { path = "../gix-hash", features = ["sha1", "sha256"] }
gix-testtools = { path = "../tests/tools", default-features = false, features = ["sha1", "sha256"] }
gix-odb = { path = "../gix-odb", features = ["sha1", "sha256"] }
gix-discover = { path = "../gix-discover" }

[package.metadata.docs.rs]
features = ["sha1", "document-features", "serde"]

[package.metadata.cargo-machete]
ignored = [
    # Forwarding-only dependency for test feature parity after dissolving gix-worktree-tests.
    "gix-features",
]
